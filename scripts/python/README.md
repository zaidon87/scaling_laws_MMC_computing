# Python Helpers

This folder is reserved for future optional helpers, for example:

- exporting workbook tables to CSV,
- checking figure file hashes,
- building a manuscript/release manifest.

The current numerical workflow is MATLAB-first.
