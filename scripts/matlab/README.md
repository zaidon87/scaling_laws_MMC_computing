# MATLAB Scripts

Main entry point:

```matlab
run('scripts/matlab/run_all_scaling_figures.m')
```

The script `mmc_scaling_analysis.m` contains the parametric scaling-law model and local helper functions.
